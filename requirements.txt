
certifi==2020.6.20
chardet==4.0.0  # Updated to a compatible version
click==8.0.4  # Updated to a compatible version
Flask==2.2.3  # Updated to a compatible version
face-recognition==1.3.0
face-recognition-models==0.3.0
opencv-python==4.7.0.72  # Updated to a compatible version
idna==3.4  # Updated to a compatible version
itsdangerous==2.1.2  # Updated to a compatible version
Jinja2==3.1.2  # Updated to a compatible version
joblib==1.1.0  # Updated to a compatible version
jsonify==1.3.0  # Updated to a compatible version
MarkupSafe>=2.1.1  # Updated to a compatible version
streamlit==1.23.1
pyinstaller==5.13.0
numpy==1.24.3  # Updated to a compatible version
pandas==1.5.3  # Updated to a compatible version
klib==1.0.0  # Specify a compatible version
pickleshare==0.7.5
requests==2.28.1  # Updated to a compatible version
scipy==1.10.1  # Updated to a compatible version
scikit-learn==1.2.2
urllib3==1.26.15  # Updated to a compatible version
Werkzeug==2.2.3  # Updated to a compatible version
wincertstore==0.2
gunicorn==20.1.0  # Specify a compatible version
